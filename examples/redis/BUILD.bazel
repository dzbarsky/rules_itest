load("@rules_itest//:itest.bzl", "itest_service")

itest_service(
    name = "redis",
    args = [
        "--port",
        "0",
        "--unixsocket",
        "$${TMPDIR}/redis.sock",
        "--unixsocketperm",
        "770",
        "--dir",
        "$${TMPDIR}",
    ],
    exe = "@redis//:redis-server",
    health_check = "@redis//:redis-cli",
    health_check_args = [
        "-s",
        "$${TMPDIR}/redis.sock",
        "PING",
    ],
)

itest_service(
    name = "redis_network",
    args = [
        "--port",
        "$${PORT}",
        "--dir",
        "$${TMPDIR}",
    ],
    autoassign_port = True,
    exe = "@redis//:redis-server",
    health_check = "@redis//:redis-cli",
    health_check_args = [
        "-h",
        "localhost",
        "-p",
        "$${PORT}",
        "PING",
    ],
    tags = [
        "requires-network",
    ],
    visibility = ["//visibility:public"],
)
